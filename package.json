{
  "name": "hp-ai-image-generator-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "install:frontend": "cd frontend && npm install",
    "clean": "cd frontend && rm -rf node_modules package-lock.json && npm cache clean --force"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}